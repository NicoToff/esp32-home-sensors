enum SensorLocation
{
    ATTIC = 1,
    OFFICE = 2
};

enum SensorValueType
{
    TEMPERATURE = 1
};

enum MillisecondTimings
{
    ONE_SECOND = 1000,
    TEN_SECONDS = 10000,
    ONE_MINUTE = 60000,
    TEN_MINUTES = 600000,
    ONE_HOUR = 3600000
};